%li{:id => ['invitation', invitation.id]}
  = "#{invitation.user.name} has sent an invitation to join #{invitation.project.name}"
  = link_to 'Accept', invitation_path(invitation), :method => :put, :class => 'button'
  = link_to 'Dismiss', invitation_path(invitation), :method => :delete, :class => 'button'
