%h2 My Account
%dl
  %dt
    %strong Name
  %dd= @user.name
  %dt
    %strong Email
  %dd= @user.email
=link_to 'Edit', edit_account_path, :class => 'btn'
=link_to 'Change Password', edit_password_path, :class => 'btn'
=link_to 'Delete Account', account_path, :class => 'btn btn-danger', :method => :delete, :confirm => 'Are you sure?'
