- if @invitations.any?
  %h2 Invitations
  %ul#invitation_list
    = render @invitations
%h2 Projects
%table#projects_list
  %thead
    %th.project_name Project
    - @statuses.each do |status|
      %th{ :class => status.alias }= status.name
    %th
    %th
  %tbody
    = render @projects, :statuses => @statuses
- if @projects.empty?
  %div.empty_list No projects yet
=link_to 'New Project', new_project_path, :class => 'button'

